model MatchOfficial {
  id                String      @id @default(cuid())

  matchOfficialId   String      @unique

  matchId           String

  officialId        String

  role              OfficialRole

  match             Match @relation(fields: [matchId], references: [id], onDelete: Restrict, onUpdate: Cascade)

  official          Official @relation(fields: [officialId], references: [id], onDelete: Restrict, onUpdate: Cascade)

  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt

  @@index([matchId])
  @@index([officialId])
}